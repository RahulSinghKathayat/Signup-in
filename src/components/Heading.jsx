import React from "react";

export const Heading = ({label}) => {
    return <div className="relative font-display text-white text-5xl font-semibold tracking-tight text-black sm:text-7xl">
        {label}
    </div>
}